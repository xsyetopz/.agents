interface:
  display_name: "Git CI/CD"
  short_description: "Build secure, blocking CI/CD pipelines"
  default_prompt: "Use $git-ci-cd to design or repair this pipeline, expose every check result, and verify trust boundaries, permissions, immutable dependencies, and real pipeline behavior."
