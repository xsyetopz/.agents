interface:
  display_name: "Git Actions"
  short_description: "Use GitHub and GitLab APIs safely"
  default_prompt: "Use $git-actions for this GitHub or GitLab API task with minimum permissions, explicit mutation authority, schema validation, verified remote effects, and a report of the result."
