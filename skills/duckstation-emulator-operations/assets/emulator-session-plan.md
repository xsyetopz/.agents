# DuckStation session plan

## Objective

- Question or acceptance condition:
- Route: interactive / automated test / regression / capture / debugger / texture / build
- Authorized mutations and external writes:

## Runtime identity

- Date and operator:
- OS, architecture, display server, and container/VM:
- GPU and driver:
- DuckStation version, channel, package, and source commit:
- Executable path and command:
- Normal or disposable portable data root:

## Console and game identity

- BIOS region, version, and SHA-256:
- Game title, region, serial, and revision:
- Image path/type, track layout, and SHA-256:
- Executable identity when using direct PS-X EXE boot:

## Configuration

- Global configuration snapshot or diff:
- Per-game configuration snapshot or diff:
- Renderer, adapter, resolution, display, and PGXP options:
- CPU execution mode, overclock, frame pacing, and speed controls:
- Cheats, enhancements, runahead/rewind, and texture replacement:
- Controller mapping and input source:
- Memory-card fixture and hash:
- Save-state producer version and slot/file, if used:

## Procedure and oracle

- Boot mode: fast / slow / BIOS / image / EXE / resume / state
- Deterministic input sequence or workload:
- Timeout and checkpoint:
- Expected visual, audio, memory, register, log, or performance result:
- Repetitions and tolerance:

## Runtime investigation

- PS-X EXE/module load mapping:
- Static file offset or virtual address:
- Runtime console address and symbol:
- Integrated debugger or GDB endpoint:
- Breakpoint/watchpoint and trigger:
- Registers/memory before and after:
- TTY/PCDrv evidence:
- Hypothesis, mutation, and rollback:

## Evidence and result

- Exit status and duration:
- Log path and relevant timestamps:
- Screenshot/video path:
- Texture dump/replacement paths and hashes:
- Configuration and command archive:
- Result: pass / fail / inconclusive
- Deviations, contamination, and follow-up:
