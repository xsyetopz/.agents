interface:
  display_name: "Prompt Engineering"
  short_description: "Design and behaviorally test prompts using current official model guidance."
  default_prompt: "Use $prompt-engineering to audit or revise this prompt. Fetch current official guidance for the named model, state each rule once, and prove the result with paired real-model behavior tests plus static source audits."
