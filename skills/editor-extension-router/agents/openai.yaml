interface:
  display_name: "Editor Extension Router"
  short_description: "Choose and coordinate editor extension platforms"
  default_prompt: "Use $editor-extension-router to select the right editor platform or design a multi-editor extension architecture, then route each implementation slice to its dedicated skill."
